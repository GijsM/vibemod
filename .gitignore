# --- Paper server runtime (downloaded/generated by scripts/setup.sh) ---
server/*.jar
server/cache/
server/libraries/
server/versions/
server/world*/
server/logs/
server/crash-reports/

# Holds the OpenRouter API key once VibeCore writes it back — never commit.
server/plugins/VibeCore/config.yml

server/plugins/.paper-remapped/

# --- Maven build output ---
plugin/target/

# --- Node tooling for scripts/ ---
scripts/node_modules/
scripts/.rcon-password

# --- misc ---
.DS_Store
*.log

# Keep these tracked even though they live under server/:
!server/eula.txt
!server/server.properties

# server runtime state (keep eula.txt + server.properties + generated mod sources)
server/.pid
server/banned-*.json
server/ops.json
server/whitelist.json
server/usercache.json
server/version_history.json
server/bukkit.yml
server/spigot.yml
server/commands.yml
server/help.yml
server/permissions.yml
server/config/
server/plugins/bStats/
server/plugins/*.jar
server/plugins/VibeCore/exports/*.jar
server/plugins/spark/
